{% from "renderer.html.njk" import mount_toggle %}

<main class="p-2 flex flex-column gap-2">
    {% for id in ["t1", "t2", "t3", "t4"] %}
        {{ mount_toggle({ "id": id, "state": false }) }}
    {% endfor %}
</main>
